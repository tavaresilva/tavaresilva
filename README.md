
function setup() {
  createCanvas(700, 400);
  background("rgb(255,255,255)")
  
}

function draw() {
  

  if (mouseIsPressed === true) { 
    
    circle(mouseX, mouseY, 20,20);
    strokeWeight(5)
    
   
}
    
  

}
